import { Card, CardContent } from "@/components/ui/card";
const SerialMonitor = () => {
  return (
    <Card className="col-span-2 row-span-2">
      <CardContent className=" font-mono">serial monitor</CardContent>
    </Card>
  );
};

export { SerialMonitor };
